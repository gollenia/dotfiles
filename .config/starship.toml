add_newline = true

[line_break]
disabled = true

# Replace the "❯" symbol in the prompt with "➜"
[package]
disabled = true
[username]
format = "[$user]($style)"

[hostname]
format = "[$ssh_symbol$hostname]($style)"
ssh_symbol = "🌏"

[cmd_duration]
disabled = true

[rust]
format = "[$symbol]($style)"
